<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 서드파티 커스텀 TTS 엔진을 앱에서 인식하고 사용할 수 있도록 권한 추가 -->
    <queries>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
    </queries>

    <uses-permission android:name="android.permission.DOWNLOAD_WITHOUT_NOTIFICATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission
        android:name="android.permission.REQUEST_INSTALL_PACKAGES"
        tools:ignore="RequestInstallPackagesPolicy" />

    <!-- 앱이 백그라운드로 가도 TTS(WebView)가 끊기지 않도록 하는 포어그라운드 서비스 권한 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:icon="@drawable/novelregex_icon_exact_v2"
        android:label="@string/app_name"
        android:roundIcon="@drawable/novelregex_icon_exact_v2"
        android:supportsRtl="true"
        android:theme="@style/Theme.NovelRegEx">

        <!-- 미디어 플레이어 포어그라운드 서비스 -->
        <service
            android:name=".activity.TtsService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="false" />

        <receiver
            android:name=".receiver.DownloadReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.DOWNLOAD_COMPLETE" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".receiver.UpdateDownloadReceiver"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".activity.SettingsActivity"
            android:exported="false"
            android:label="@string/title_activity_settings"
            android:parentActivityName=".activity.MainActivity" />

        <activity
            android:name=".activity.BookmarksActivity"
            android:exported="false"
            android:label="@string/title_activity_bookmarks"
            android:parentActivityName=".activity.MainActivity" />

        <activity
            android:name=".activity.BookmarkOrderActivity"
            android:exported="false"
            android:label="@string/title_activity_bookmark_order"
            android:parentActivityName=".activity.BookmarksActivity" />

        <activity
            android:name=".activity.UserRulesActivity"
            android:exported="false"
            android:label="사용자 규칙"
            android:parentActivityName=".activity.MainActivity" />

        <!-- TTS 정규식 설정 화면 -->
        <activity
            android:name=".activity.TtsRegexSettingsActivity"
            android:exported="false"
            android:label="TTS 정규식 설정"
            android:parentActivityName=".activity.MainActivity" />

        <activity
            android:name=".activity.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout|smallestScreenSize">   

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- autoVerify will fail, but I have to do this to register this intent filter -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="https" />
                <data android:scheme="http" />
                <data android:host="novelpia.com" />
            </intent-filter>

        </activity>

    </application>

</manifest>
